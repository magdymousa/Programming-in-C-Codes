#include <stdio.h>
#include <stdlib.h>

size_t maxSeq(int * array, size_t n);
int main(void){
  int *p[] = {{1,2,3,4,5,6,7,8},{1,3,2,4,6,9,1,4},{12,7,9,30,40,90,200,1};
   
	      if(maxSeq(0,0) != NULL){		
		EXIT_FAILURE;
	      }
	      else if(maxSeq(p[0],8) != 8){	
		EXIT_FAILURE;
	      }
	      else if(maxSeq(p[1],8) != 4){	
		EXIT_FAILURE;
	      }
	      else if(maxSeq(p[2],8) != 6){	
		EXIT_FAILURE;
	      }
	      else{	
		EXIT_SUCCESS;
	      }
  }
